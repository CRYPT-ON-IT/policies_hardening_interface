on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        dry-run: true
        log-level: verbose
        server: sftp.cluster021.hosting.ovh.net
        username: ataumophzi
        port: 21
        protocol: ftp
        local-dir: ./
        server-dir: /www/
        password: ${{ secrets.ftp_password }}
        
